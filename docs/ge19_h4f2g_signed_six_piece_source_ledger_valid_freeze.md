# GE19 H4F2g — valid signed six-piece source-row ledger (source-only)

## Exact result and scientific boundary

**Classification:**
`GE19_H4F2G_SIGNED_SIX_PIECE_SOURCE_LEDGER_PASS_FULL_NOETHER_OPEN`.

The separately versioned source assembler and deterministic
source-only audit passed across all three original
`beta={1,0.5,0.1}` cohorts. The fixed eight-row
GE19 source order is
`(N,L+R,u,phi,T,rho,b,L-R/2)`.

The ledger combines exactly six signed H4 RHS
families:

`2Q_GE06_cross`,
`2Q_GE07_cross`,
`2Q_Lambda_cross`,
`2DY2_action_complete_Y_u_and_phi`,
`2M1_GE05_mapped`,
`2M2_GE05_mapped`.

The complete Y source is **created directly from
the frozen Stage E action-derived source_h4** and
has both u and phi source rows. The old
Repair37 scalar-only DY2 family cannot be passed
into this new API or counted twice.

All five other source inputs are required to be
already in original GE19 raw RHS/fft-over-nx
convention; this standalone audit does not
authenticate a caller's arbitrary external
physical arrays. The deterministic GE06/GE07/
Lambda/M2 test arrays are explicitly synthetic,
and only Y and M1 are generated by their
real frozen numerical implementations here.
Their actual action/source definitions remain
independently certified by H4F2a–f.

**This is not a full all-sector H4 Noether
identity or a corrected-parent H4 source
evaluation. There is no Z21 solve or lensing
license.**

## Preregistration, implementation and CI

- frozen preregistration:
  `ge19/h4f2g_predata_six_piece_signed_source_ward_ledger.json`,
  blob `70bee663a0f529a734b586dbc8b9f6e90bc8ed9f`;
- assembler:
  `ge19/h4f2g_action_completed_six_piece_source_ledger.py`,
  blob `d9778da0bb6cc52a15015238810c79978527ffc5`;
- deterministic CI selftest:
  `ge19/h4f2g_six_piece_source_ledger_selftest.py`,
  blob `eb33a29e4be5975cef65b5ea62ac94b79d51114d`;
- dedicated workflow:
  `.github/workflows/ge19-h4f2g-six-piece-source-ledger.yml`;
- valid Actions commit:
  `97819c0276fcd600510492e3e5ce571de3c30861`;
- run:
  `36049303037`;
- job:
  `107800548909`;
- conclusion:
  `success`;
- terminal marker:
  `GE19_H4F2G_SIX_PIECE_SIGNED_SOURCE_LEDGER_PASS`;
- artifact ID:
  `10830265260`;
- JSON:
  `results/ge19_h4f2g_signed_six_piece_source_ledger.json`;
- JSON bytes:
  `8172`;
- JSON SHA-256:
  `34151f1e886477f1a08546d9a493ee9e418a8890d72c006d9f7e26583176596d`.

All pinned source blobs, actual source-implementation
bindings, three source-only beta cohorts, negative
input tests, exact Y row generation, original M1
mapping and signed source Ward aggregation passed.
No science threshold was relaxed.

## The signed source Ward remainder

The H4F2b formal coefficient uses physical
`partial_eta partial_epsilon^2`, not a
factorially rescaled quadratic coefficient.
In the original GE19 row convention, define
for a full six-piece source

`D_S21 = partial_t S_b21+
(a(t)/3) partial_x(S_iso21+2 S_aniso21)`.

The source assembler returns `D_S21` and
each piece's independently computed
contribution using one supplied strictly
increasing time grid and the same fixed
Fourier wave numbers.

The deterministic test verifies that
the total is the sum of all six
piece projections. It does **not**
impose `D_S21=0`. In general, the
actual on-shell source identity also
contains the Ward variation of the
linear operator and signed background,
H1, Z11, H3F Z20, H3G q20, GE07 dust
and GE05 per-node bath parent terms.

For the source representation verified
in this audit, Y and M1 have identically
zero shift/isotropic/anisotropy rows
and therefore `D_S21,Y=D_S21,M1=0`
exactly. Lambda has zero **direct**
shift source but a nonzero isotropic
source and generally a nonzero
`D_S21,Lambda`; it must not be
dropped from the aggregate.

The synthetic test preserves nonzero
GE07 shift and GE05 M2 anisotropy rows.
It rejects a legacy DY2 extra family,
missing source or constraint array,
wrong row support, nonfinite arrays
and mismatched/nonmonotonic grids.
A deliberate external Fourier amplitude
error is detectable **against the
independent synthetic reference**, not
by an untrusted bare source array alone.

## Exact physical parent boundary

Corrected-H3F Z20 and H3G weighted q20
are independently certified and frozen,
but no actual common H3F/H3G/H1/Z11
parent fields were loaded or resampled
by this source-only test. The original
Repair37 build_source_config still
uses historical Repair22/Repair27
parents and its scalar-only DY2.
It **cannot** be relabelled as the
new corrected all-sector H4 source
or used unmodified for new Z21 science.

A historical H4F2 preregistration
sentence requiring per-node q20 states
for the *implemented M1* is
too strong: the actual mapped M1
uses `B20=X20-weighted_z20`.
This was established by frozen H4F2d.
The complete **off-shell bath Noether
identity** still requires per-node
bath Euler equations, and the M2
first-order source uses the actual
q10 bath node states.

## Next licensed work

Preregister a separate common corrected-parent/
time **full signed Ward source-plus-operator/
parent-residual evaluation**. It must first
bind verified H1, compatible frozen Z11,
corrected H3F Z20, H3G weighted q20 and
the first-order Repair26 bath nodes under
exact original source/time/derivative
conventions. Verify the genuine GE06,
GE07, Lambda and GE05 M2 sources
against their actual frozen generators
on that *same* representation.

Evaluate `D_S21`, the independently
derived linear-operator Ward expression,
all signed parent Euler residuals,
and boundary terms at the preregistered
early-window hotspot and across the
full original active window. Preserve
the unchanged `1e-6` active-shift
science target. Do not fit a source
offset or silently combine FD8 and FD4
without a discrete-identity audit.

Only a separately frozen full
source-plus-parent compatibility PASS
could license one preregistered
science H4/Z21 reclosure.

**Full mixed H4 Noether NOT CERTIFIED.
Z21 NOT CERTIFIED. Lensing blocked.**
