# NL1B2 result — directional second-order eta-tangent closure

Final classification: **NL1B2_DIRECTIONAL_SECOND_ORDER_ETA_TANGENT_PASS**

Predata commit: `725d86904b3e5697dcc0516eb21144bcce8c3929`.

Scope: analytic theory closure only. No observational data, no finite-eta nonlinear simulation, and no weakly nonlinear sign outcome were used.

Historical `NL1B_SECOND_ORDER_DYNAMICAL_CLOSURE_INCOMPLETE` remains unchanged. NL1B2 is a narrower directional closure selected after that incompleteness was recorded and before any nonlinear eta-tangent result.

## 1. Separate physical state from the normalized bath response

For clarity the exact hierarchy is most transparent if the physical scalar-projected AeST/metric/matter state is denoted `Z`, while `q` denotes the normalized NL0B auxiliary response after

\[
U_j=\sqrt\eta\,q_j.
\]

The exact equations can then be written schematically as

\[
\mathscr E_0[Z]+\eta\,\mathscr M[Z,q]=0,
\]

\[
\mathscr G[Z,q]=0.
\]

`E0` is the memory-off frozen AeST+matter system including the NL0C Y-sector. `M` is the coefficient of physical eta generated by the completed-square NL0B action. `G` is the normalized retarded bath equation. Importantly, after the `U=sqrt(eta) q` normalization, `G` has a finite eta->0 limit and contains no factor of eta.

This split is algebraically equivalent to the preregistered notation and removes a bookkeeping ambiguity: the raw bath exists as a response field at eta=0, while its backreaction on the physical fields is multiplied by eta.

## 2. Two-parameter expansion

Use

\[
Z=\bar Z+\epsilon Z_{10}+\eta\epsilon Z_{11}
+\frac{\epsilon^2}{2}Z_{20}
+\frac{\eta\epsilon^2}{2}Z_{21}+\cdots,
\]

and

\[
q=\epsilon q_{10}+\eta\epsilon q_{11}
+\frac{\epsilon^2}{2}q_{20}
+\frac{\eta\epsilon^2}{2}q_{21}+\cdots.
\]

The target is

\[
Z_{21}=\left.\partial_\eta Z^{(2)}\right|_{\eta=0}.
\]

No finite physical eta is introduced.

## 3. Analytic versus nonanalytic memory-off operator

Split

\[
\mathscr E_0=\mathscr E_{an}+\mathscr E_Y,
\]

where `E_an` is analytic around FLRW and `E_Y` is the NL0C small-gradient contribution.

Define

\[
L=D\mathscr E_{an}[\bar Z],
\]

\[
Q(V,W)=D^2\mathscr E_{an}[\bar Z](V,W).
\]

The leading Y residual is the homogeneous directional map

\[
Y_2[V]=\lim_{s\downarrow0}s^{-2}
\mathscr E_Y[\bar Z+sV].
\]

This is required because the `Y^(3/2)` branch is not represented by one field-independent bilinear Hessian at the zero-gradient FLRW state.

## 4. H1 — baseline first order

The coefficient of epsilon is

\[
\boxed{L Z_{10}=0}.
\]

This is exactly the frozen memory-free linear AeST system. The Y-sector is absent because `J_Y(0)=0`.

**H1: PASS.**

## 5. H2 — first-order eta tangent

At coefficient `eta epsilon`, the physical equation is

\[
\boxed{
L Z_{11}=-M_1[Z_{10},q_{10}]
}.
\]

The normalized bath coefficient is fixed independently by the linearization of `G`:

\[
G_q q_{10}+G_Z Z_{10}=0.
\]

For the scalar longitudinal mode this is exactly

\[
q_j''+2\mathcal Hq_j'+a^2\omega_j^2q_j
=ak\omega_j\sqrt{w_j}\chi_{10},
\]

and the physical aether closure receives

\[
\Delta E_{rhs}^{(\eta)}
=-\frac{Q}{2}B_{\chi,raw}[\chi_{10},q_{10}].
\]

This is the same eta=0 tangent system certified numerically by v0.77.

**H2: PASS.**

## 6. H3 — baseline directional second order

For the analytic sector,

\[
\mathscr E_{an}[Z]
=\frac{\epsilon^2}{2}
\left[LZ_{20}+Q(Z_{10},Z_{10})\right]+\cdots.
\]

The Y-sector contributes

\[
\mathscr E_Y[Z]=\epsilon^2Y_2[Z_{10}]+O(\epsilon^3).
\]

Therefore

\[
\boxed{
L Z_{20}
=-Q(Z_{10},Z_{10})-2Y_2[Z_{10}]
}.
\]

The factor two is fixed by the convention `Z=Zbar+epsilon Z10+epsilon^2 Z20/2+...`.

**H3: PASS.**

## 7. Exact NL0C directional Y source

For the scalar weak-field spatial part let

\[
\mathbf g=\nabla_{phys}\chi_{10}.
\]

Then

\[
Y_2[\chi_{10}]
=\frac{2(2-K_B)}{(1+\beta_0)a_0}
\nabla_{phys}\cdot(|\mathbf g|\mathbf g).
\]

This is exactly the NL0C/NL1A operator.

## 8. Directional derivative of the nonanalytic Y source

Let

\[
\mathbf h=\nabla_{phys}\chi_{11}.
\]

For `|g|>0`,

\[
D(|\mathbf g|\mathbf g)[\mathbf h]
=|\mathbf g|\mathbf h
+\frac{\mathbf g\cdot\mathbf h}{|\mathbf g|}\mathbf g.
\]

At `g=0`, the directional derivative is zero because

\[
|s\mathbf h|(s\mathbf h)=O(s^2).
\]

Hence

\[
\boxed{
DY_2[\chi_{10};\chi_{11}]
=\frac{2(2-K_B)}{(1+\beta_0)a_0}
\nabla_{phys}\cdot\left[
|\mathbf g|\mathbf h+
\frac{\mathbf g\cdot\mathbf h}{|\mathbf g|}\mathbf g
\right]
}
\]

with the bracket set continuously to zero where `|g|=0`.

This gives a concrete machine-checkable NL1C operator identity; it is not an assumed `F2` kernel.

**Y directional identity: PASS.**

## 9. H4 — second-order eta tangent

Differentiate the second-order physical equation with respect to eta at eta=0. The analytic memory-off residual gives the cross Hessian

\[
2Q(Z_{10},Z_{11}),
\]

and the Y-sector gives

\[
2DY_2[Z_{10};Z_{11}].
\]

Because the physical memory residual is `eta M[Z,q]`, its eta derivative at eta=0 samples the **baseline eta=0 fields**, not the eta tangent of those fields. Expanding `M` through second perturbative order gives

\[
\mathscr M[Z(\eta=0),q(\eta=0)]
=\epsilon M_1[Z_{10},q_{10}]
+\frac{\epsilon^2}{2}
\left(
M_1[Z_{20},q_{20}]
+M_2[(Z_{10},q_{10}),(Z_{10},q_{10})]
\right)+\cdots.
\]

Therefore the exact directional second-order eta-tangent equation is

\[
\boxed{
\begin{aligned}
L Z_{21}={}&-2Q(Z_{10},Z_{11})
-2DY_2[Z_{10};Z_{11}]\\
&-M_1[Z_{20},q_{20}]
-M_2[(Z_{10},q_{10}),(Z_{10},q_{10})].
\end{aligned}
}
\]

This is the preregistered H4 structure with the normalized bath bookkeeping made explicit.

The baseline second-order normalized bath `q20` is fixed by the second directional expansion of the eta-independent retarded equation `G[Z,q]=0`. The eta-tangent bath fields `q11,q21` are required if the auxiliary response itself is reported, but they do not enter the coefficient of the physical memory force at first order in eta because that force already carries the explicit outer factor eta.

**H4: PASS.**

## 10. Memory quadratic vertices are fixed without a Fourier nonlinearization

`M1` and `M2` are, by definition, the first and second perturbative directional coefficients of the exact NL0B covariant memory residual after `U=sqrt(eta)q`.

Consequently `M2` automatically contains every contribution from

- the nonlinear spatial projector;
- the nonlinear `X_mu` dictionary;
- the metric contractions;
- the projected aether derivative;
- the invariant volume;
- the auxiliary orthogonality condition;
- the direct memory Einstein stress beginning at second order.

No rule of the form “replace k chi by a nonlinear Fourier convolution” is permitted or needed.

This is a complete operator definition even though the expanded component expressions are intentionally deferred to the numerical generator.

**Memory directional closure: PASS.**

## 11. Scalar projection

Scalar-scalar products do generate vector and tensor fields at `O(epsilon^2)`. However the first-order SVT operator on FLRW is block diagonal. An `O(epsilon^2)` vector/tensor solution cannot feed the scalar equations through the linear operator until `O(epsilon^3)`.

Therefore projecting the quadratic sources onto the scalar sector gives a closed scalar second-order hierarchy for the order targeted here. This does not assert that the generated vector/tensor observables vanish.

**Scalar projection: PASS.**

## 12. Noether / constraint inheritance

Let the exact diffeomorphism Noether identity be schematically

\[
\mathcal N^I[Z]\mathscr E_I[Z]\equiv0.
\]

Taking the same epsilon and eta directional derivatives that define H1-H4 produces the order-by-order constraint identities. Therefore the source combinations `Q`, `Y2`, `M1` and `M2` cannot be selected independently: when all are generated from the same frozen action they obey the differentiated identity automatically.

The later numerical solver must still monitor an independent scalar constraint residual, but no additional theory coefficient is required to make the hierarchy consistent.

**Noether inheritance: PASS.**

## 13. No new freedom

The hierarchy depends only on the already frozen AeST parameters/background, `a0`, the co-primary `beta0` values and the preserved positive Drude spectrum. The physical target is a derivative at eta=0, so no finite eta value is introduced.

**No-new-freedom check: PASS.**

# Classification

All NL1B2 preregistered analytic checks close:

\[
\boxed{\text{NL1B2\_DIRECTIONAL\_SECOND\_ORDER\_ETA\_TANGENT\_PASS}}
\]

## What is now allowed

The first nonlinear numerical result may target the directional eta derivative of a weakly nonlinear source/response at eta=0. Before any sign is interpreted, the implementation must verify the explicit `DY2` identity against finite differences of the already certified NL1A pseudospectral operator and must retain the v0.77/v0.78 trusted linear state path.

## What is not claimed

- historical NL1B is not rescued;
- no finite-eta nonlinear prediction is certified;
- no halo or N-body result exists yet;
- no observational detection is claimed;
- the full component-expanded relativistic second-order FLRW equations are still not written as a standalone closed-form list.

NL1B2 instead freezes an exact functional/directional route to the first strong nonlinear memory test without inserting a phenomenological gravitational closure.
