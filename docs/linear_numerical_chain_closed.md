# Linear numerical chain — CLOSED

This document records the closed status of the linear AeST+memory numerical chain after v0.77 and v0.78. It is a standing project constraint for all later nonlinear work.

## Certified result

The native total-matter perturbation state `d_m(k,z)` has a numerically certified eta=0 retarded-memory tangent in the frozen AeST model.

Historical classifications remain unchanged. In particular, all earlier FAIL classifications remain FAIL and are not retroactively rescued.

### v0.77

`V077_NATIVE_STATE_TANGENT_AFFINITY_PASS`

Diagnosis:

`V077_FRACTIONAL_ZERO_CONDITIONING_SUPPORTED`

Primary additive state tangent:

`T_lambda = [d_m(+lambda)-d_m(-lambda)]/(2 lambda)`

The primary metric uses global L2 normalization and never divides by a local `d_m` or `P_m` value.

Maximum lambda-affinity error in the native window was approximately `2.254e-4` (`0.0225%`), well below the preregistered `5e-3` limit. The independent even-residual test also passed.

Interpretation: the memory-induced linear signal exists in the perturbation state itself and is not generated by local fractional normalization near a transfer zero/minimum.

### v0.78

`V078_TIME_INTERPOLATION_OPERATOR_CLOSURE_PASS`

Diagnosis:

`V078_TIME_INTERPOLATION_OPERATOR_DISCREPANCY_LOCALIZED`

At common native `(k,tau)` nodes, the source-to-power identity

`P_m = (2 pi^2/k^3) d_m^2 P_R`

closes against the CLASS Fourier linear power to machine precision, with maximum relative discrepancy of order `1e-14`.

At the same native k values but off-native times, the source-interpolated route and the CLASS Fourier/log-power interpolation route can differ strongly in the highly structured AeST regime. This localizes the historical `pk_lin` discrepancy to the off-native time-interpolation operator rather than to perturbation evolution, normalization, primordial prefactors, source-to-power construction, or k-resolution.

## Standing interpretation

The project therefore treats the following as closed numerical facts:

1. The eta=0 retarded-memory forcing produces a reproducible, scale- and time-dependent response in the native total-matter perturbation `d_m(k,z)`.
2. The additive state tangent is numerically lambda-affine in the tested native window.
3. The standard source-to-power relation is correct on native nodes.
4. The previous large `pk_lin` discrepancies are an off-native time-interpolation effect in a strongly structured transfer function, not evidence against the underlying perturbation signal.
5. No further CLASS interpolation debugging is part of the main scientific path unless a later independent result specifically requires it.

## Scope limits

This closure does **not** establish observational detection, nonlinear saturation, nonlinear collapse, halo formation, or a full nonlinear AeST+memory solution.

It establishes a certified linear-theory signal and a trusted linear state representation from which nonlinear work may proceed.

## Next phase

The project now moves to the nonlinear path. The target question is no longer whether the linear signal exists, but whether the certified memory-induced change in `d_m(k,z)` propagates into nonlinear mode coupling, collapse/onset, and ultimately observable structure formation.
