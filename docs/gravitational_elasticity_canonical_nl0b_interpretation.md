# Gravitational Elasticity — canonical NL0B interpretation

## Status

This note consolidates the physics-first gravitational-elasticity interpretation with the already certified covariant memory completion.

The canonical active model is **not** a new shear-coupled theory.

It is the existing frozen NL0B local conservative action, which already passed the full covariant-memory audit and exactly reproduces the certified linear CLASS closure.

The separate shear-strain action proposed in `docs/gravitational_elasticity_conservative_completion.md` is retained as an exploratory alternative only. It is not the active theory unless a future preregistered comparison explicitly promotes it.

## 1. Frozen covariant action

Use the existing AeST fields

`g_{mu nu}, A_mu, phi`

and

`h_{mu nu}=g_{mu nu}+A_mu A_nu`.

The gravitational deformation driver is the aether-orthogonal scalar-gradient covector

`X_mu=h_mu^nu nabla_nu phi`.

For each positive internal mode introduce

`A^mu U_{j mu}=0`

and

`D_A U_{j mu}=h_mu^nu A^rho nabla_rho U_{j nu}`.

The frozen NL0B action is

`S_mem=(16 pi Gtilde)^(-1) integral sqrt(-g) (1/4) sum_j [
 |D_A U_j|^2
 - |omega_j U_j-sqrt(eta w_j) X|^2
]`.

All

`w_j>0`

and

`omega_j>0`.

NL0B already established:

- the AeST perturbation dictionary;
- exact FLRW null;
- exact oscillator reduction;
- exact backreaction variable;
- exact frozen E closure;
- no direct linear Einstein stress about FLRW;
- no extra independent first-order scalar constraint;
- exact positive Drude transfer identity.

Therefore this action is the canonical elastic completion.

## 2. Elastic strain mismatch

Define the physical elastic mismatch

`e_{j mu}=sqrt(eta w_j) X_mu-omega_j U_{j mu}`.

Then the mode action is

`L_j=(1/4)[|D_A U_j|^2-|e_j|^2]`.

The positive local elastic potential-energy density is proportional to

`|e_j|^2/4`.

This gives the direct mechanical interpretation:

1. gravity changes `X_mu`;
2. the internal coordinate `U_j` cannot adjust instantaneously;
3. a mismatch `e_j` is created;
4. the mismatch stores elastic energy;
5. variation of that stored energy returns a force to the gravitational/aether sector;
6. the internal oscillator subsequently relaxes/oscillates toward the driven state.

Thus the memory force is an elastic restoring force generated by delayed internal deformation.

## 3. Backreaction is the elastic stress variable

With the normalized field

`U_j=sqrt(eta) q_j`,

define

`B_mu=sum_j [w_j X_mu-omega_j sqrt(w_j) q_{j mu}]`.

The mismatch satisfies

`e_{j mu}=sqrt(eta)[sqrt(w_j) X_mu-omega_j q_{j mu}]`.

Therefore

`B_mu=(1/sqrt(eta)) sum_j sqrt(w_j) e_{j mu}`

for eta>0, with a regular normalized eta->0 tangent.

The already frozen AeST force

`Delta E_rhs=-(eta Q/2) B_chi,raw`

is therefore exactly the longitudinal scalar projection of the elastic restoring force.

This is not an analogy added after the fact. It follows algebraically from the completed-square action.

## 4. One-mode mechanics

For one mode in a local Minkowski frame,

`ddot U+omega_E^2 U=sqrt(eta w_E) omega_E X`.

The driven equilibrium is

`U_eq=sqrt(eta w_E) X/omega_E`.

The elastic mismatch is

`e=sqrt(eta w_E)X-omega_E U`.

If X changes suddenly, U remains continuous and e becomes nonzero immediately.

If X is held constant, U evolves toward/around the driven equilibrium according to the conservative oscillator dynamics.

With an expanding FLRW background,

`ddot U+3H dot U+omega_E^2 U=sqrt(eta w_E) omega_E X`.

The Hubble term damps the transient without introducing an ad-hoc microscopic dissipation law.

## 5. Static and dynamic meaning

The static limit of the preserved Drude response is zero:

`B/X -> 0` as forcing frequency -> 0.

This is physically important.

The model is not an additional static Hooke-law modification of Newtonian gravity.

It is a **relaxing gravitational elasticity**:

- slow/constant gravitational deformation is followed by the internal coordinate and leaves no residual mismatch;
- changing gravitational deformation produces a temporary restoring force;
- rapid forcing sees a finite unrelaxed modulus.

This is precisely why the model naturally produces gravitational memory without changing the homogeneous static background.

## 6. Exact one-mode transfer function

For fixed H and Laplace frequency s,

`q(s)=omega_E sqrt(w_E) X(s)/(s^2+3Hs+omega_E^2)`.

Hence

`B(s)/X(s)
 =w_E s(s+3H)/(s(s+3H)+omega_E^2)`.

Define

`K_E(s;H)=w_E s(s+3H)/(s(s+3H)+omega_E^2)`.

Then:

### relaxed limit

`K_E(0;H)=0`.

### high-frequency limit

`K_E -> w_E`.

### stable homogeneous poles

`s_pm=(-3H +/- sqrt(9H^2-4omega_E^2))/2`.

For

`H>=0`

and

`omega_E^2>0`,

both poles have non-positive real part.

At H=0 they are purely oscillatory:

`s=+/- i omega_E`.

No exponentially growing internal elastic mode is introduced.

## 7. Energy positivity of the added mode

For fixed external X, the internal mode energy is

`E_j=(1/4)[|D_A U_j|^2+|omega_j U_j-sqrt(eta w_j)X|^2]`.

For positive spatial metric, positive omega_j^2 and eta,w_j>=0,

`E_j>=0`.

At zero source X=0,

`E_j=(1/4)[|D_A U_j|^2+omega_j^2|U_j|^2]`.

Thus the added auxiliary mode itself has:

- positive kinetic energy;
- positive restoring energy;
- no homogeneous exponential instability.

This statement concerns the added NL0B sector. Stability of the complete coupled AeST system remains the already separately audited baseline-theory question.

## 8. Exact zero-coupling edge

At

`eta=0`

the physical auxiliary field decouples.

The memory action reduces to an independent positive oscillator sector and the physical AeST equations reduce exactly to the frozen eta=0 baseline.

The normalized response variable q can remain finite for tangent calculations, but its physical backreaction is multiplied by eta.

Therefore

`eta=0`

is an exact theory edge, not a small numerical approximation.

## 9. Relation to existing project results

This interpretation uses rather than replaces the successful project chain:

- NL0B: covariant memory completion PASS;
- NL0C: weakly nonlinear Y-sector PASS;
- NL1A: pseudospectral Y-operator PASS;
- NL1B2: directional second-order eta-tangent PASS;
- NL1C5: spherical variational bridge PASS;
- NL1C6: spherical self-gravity closure PASS.

The B4-B8 sequence is a failed/blocked cosmological nonlinear initial-data construction within that larger theory.

It is not evidence that the elastic action itself is inconsistent.

## 10. Immediate program

Stop further B4-B8 numerical repair.

The next physics result should use the already certified action in the simplest regime where the elastic interpretation is visible:

1. one-mode analytic frequency/step response;
2. one-mode linear cosmological response using the existing CLASS infrastructure;
3. weak-field time-dependent spherical forcing;
4. finite-amplitude nonlinear evolution only after those tests are clear.

The first result requires no new field theory and almost no numerics.
