# NL1B predata — second-order AeST+memory dynamical closure

Classification before derivation: **NL1B_PREDATA_SECOND_ORDER_DYNAMICAL_CLOSURE**

This is a theory-only preregistration created after the NL1A operator PASS and before any weakly nonlinear growth result. It uses no observational data and no outcome-dependent parameter choice.

Historical results remain immutable: v0.77 PASS, v0.78 PASS, NL0B PASS, NL0C PASS, NL1A PASS, and all earlier FAIL classifications retain their original status.

## 1. Scientific purpose

Freeze the actual second-order scalar-sector evolution equations that will be used in the first physical weakly nonlinear AeST+memory calculation.

The target is **not** a GR/Poisson toy model and not a static MOND solver. The closure must descend from the frozen covariant AeST action plus the NL0B local conservative memory completion and must retain the time-dependent scalar/aether/memory dynamics needed to connect to the certified CLASS linear state.

No nonlinear density-growth result may be generated until this gate is closed.

## 2. Frozen physical theory

Use the same frozen cosmological branch as the certified linear chain:

- `aest_model = Exp`;
- `K_B = 0.0665`;
- `Q0 = 1e-4 Mpc^-1`;
- `K2 = 9500`;
- `Z0 = 1e-17 Mpc^-1`;
- `tau H0 = 10`;
- same background calibration and cosmological parameters as v0.63/v0.77;
- same positive Drude spectrum as the preserved memory construction;
- physical memory coupling `eta >= 0`; numerical tangent amplifier `lambda` is not a theory parameter.

The free function is frozen as

\[
\mathcal F(\mathcal Y,\mathcal Q)
=-2\mathcal K(\mathcal Q)+(2-K_B)\mathcal J(\mathcal Y),
\]

with the NL0C small-gradient branch

\[
\mathcal J(\mathcal Y)
=\frac{2}{3(1+\beta_0)a_0}\mathcal Y^{3/2}+O(\mathcal Y^2),
\]

and co-primary

\[
\beta_0\in\{1,0.5,0.1\},\qquad
a_0=1.2\times10^{-10}\,{\rm m\,s^{-2}}.
\]

No interpolation-function shape is introduced in NL1B.

The memory completion is exactly the NL0B local auxiliary-vector action with

\[
X_\mu=(g_{\mu\nu}+A_\mu A_\nu)\nabla^\nu\phi,
\]

and the same positive auxiliary modes \(U_{j\mu}\). No new nonlinear memory parameter is allowed.

## 3. Perturbative target

Expand around the same FLRW background through equations of motion of order \(O(\epsilon^2)\) in the scalar sector.

The retained scalar variables must be sufficient to map continuously onto the frozen linear bridge variables

\[
\delta_A,\quad \Theta_A,\quad \alpha,\quad E,\quad \chi,
\]

plus the ordinary matter/radiation variables needed by the chosen late-time weakly nonlinear reduction and the memory auxiliary fields.

Matter remains minimally coupled. In the weak-field/subhorizon matter sector the nonlinear continuity and Euler convective terms follow from \(\nabla_\mu T_m^{\mu\nu}=0\) and are not modified by hand.

The gravitational/AeST/memory closure is the part to be derived.

## 4. Locked derivation checks

NL1B passes only if all checks below are established without changing the frozen theory after the algebra is inspected.

### B1. First-order AeST reduction

Dropping all \(O(\epsilon^2)\) terms must reproduce the frozen memory-free AeST scalar bridge, including

\[
\chi=Q(u_A+\alpha),
\]

\[
\Pi_A=c_{ad}^2\delta_A+
\frac{c_{ad}^2 k^2}{3a^2\rho_A}
\left[K_BE+(2-K_B)\chi\right],
\]

and the frozen \(\delta_A,\Theta_A,\alpha,E\) evolution equations.

### B2. First-order memory reduction

The auxiliary system must reduce exactly to

\[
q_j''+2\mathcal Hq_j'+a^2\omega_j^2q_j
=ak\omega_j\sqrt{w_j}\chi,
\]

and

\[
\Delta E_{\rm rhs}
=-\frac{\eta Q}{2}B_{\chi,\rm raw}.
\]

No additional independent first-order source is allowed.

### B3. NL0C Y-sector vertex

The complete second-order scalar equations must contain the already frozen leading Y-sector contribution

\[
\frac{2(2-K_B)}{(1+\beta_0)a_0}
\nabla_{\rm phys}\cdot
\left(|\nabla_{\rm phys}\chi|\nabla_{\rm phys}\chi\right),
\]

with the same sign and coefficient used in NL1A.

### B4. Memory quadratic vertices

Derive all \(O(\eta\epsilon^2)\) terms generated by the NL0B action. They must arise only from the frozen covariant objects \(X_\mu\), \(U_{j\mu}\), the projector, metric, aether derivative and volume element. No Fourier-space nonlinear `k chi` rule may be inserted by hand.

### B5. Constraint/Noether closure

At second order, the scalar-field, aether and Einstein equations must satisfy the diffeomorphism/Bianchi consistency relation so that the evolution system is not over-constrained and no equation omitted at first order becomes an independent missing second-order constraint.

### B6. Static weak-field control

In the memory-off static weak-field limit, the derived equations must reduce to the published AeST two-potential system

\[
\Phi=\tilde\Phi+\chi,
\]

\[
\nabla^2\tilde\Phi+\mu^2\Phi
=\frac{4\pi G_N}{1+\beta_0}\rho_b,
\]

\[
\nabla^2\tilde\Phi
=\nabla\cdot\left(\mathcal J_{\mathcal Y}\nabla\chi\right),
\]

through the order represented by the NL0C small-gradient branch.

This is a control on the covariant reduction, not the dynamical cosmological closure itself.

### B7. No new free nonlinear coefficient

Every second-order coefficient must be fixed by the frozen AeST parameters, background functions, \(a_0,\beta_0\), and the preserved Drude spectrum. Any unavoidable new coefficient or function makes NL1B incomplete and must be preregistered in a new theory branch before numerics.

### B8. Well-posed numerical state

The final reduced system must be expressible as a deterministic initial-value problem suitable for the NL1A pseudo-spectral spatial operator: evolution variables, elliptic constraints if any, and the order in which they are solved must be explicit.

### B9. Linear numerical cross-check plan frozen before growth

Before nonlinear terms are activated, the NL1B implementation must be run with the nonlinear vertices disabled and compared against the trusted native CLASS linear state in the previously certified window

\[
0.03\le k\le0.20\ h\,{\rm Mpc}^{-1},
\qquad 0.2\le z\le1.5.
\]

This cross-check is implementation validation only. Its quantitative tolerances must be fixed before the comparison result is inspected.

## 5. Classification

If B1–B9 are all closed:

**NL1B_SECOND_ORDER_DYNAMICAL_CLOSURE_PASS**

If any identity is contradicted by the frozen action:

**NL1B_SECOND_ORDER_DYNAMICAL_CLOSURE_FAIL**

If the derivation exposes an additional theory choice not fixed by NL0B/NL0C:

**NL1B_SECOND_ORDER_DYNAMICAL_CLOSURE_INCOMPLETE**

A PASS freezes the equations for NL1C, the first physical weakly nonlinear growth calculation. It does not itself claim nonlinear suppression, delay, enhancement, saturation, or observational agreement.

## 6. Integrity rule

The desired sign of the nonlinear memory effect is not part of this gate. A later NL1C result may be delay, advance, mixed, null, or numerical failure. No coefficient, beta0 value, memory spectrum, physical eta, grid, or diagnostic may be changed after seeing that outcome in order to promote one sign.
