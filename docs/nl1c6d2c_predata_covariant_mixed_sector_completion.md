# NL1C6D2C predata — covariant mixed-sector completion campaign

Status: **PREREGISTERED BEFORE ANY NL1C6D2C NONLINEAR FLRW COMPLETION RESULT OR BRANCH-EVOLUTION OUTPUT**.

Frozen label:

```text
NL1C6D2C_PREDATA_COVARIANT_MIXED_SECTOR_COMPLETION
```

## 1. Motivation and historical boundary

The chain entering this phase is

```text
NL1C6R3_FULL_J_BARYONIC_RECLOSURE_FAIL
NL1C6D1_DYNAMICAL_FORMULATION_AUDIT_PASS
NL1C6D2A_BARYON_MATTER_SECTOR_AUDIT_PASS
NL1C6D2B_FLRW_SOURCE_GRAVITY_COUPLING_AUDIT_INCOMPLETE
```

The D2B `INCOMPLETE` result is immutable. Its B1 analysis established that the already frozen homogeneous `K(Q)` sector, frozen linear CLASS trajectory, and frozen quasistatic full-`J(Y)` sector do not uniquely determine the nonlinear mixed dependence of the covariant free function `F(Y,Q)`.

D2C therefore does **not** attempt to convert the historical D2B result into a PASS. It explicitly introduces a preregistered theory-completion family and asks whether the nonlinear FLRW reduction and all inherited regressions can be closed for that family without post-result tuning.

No member of the family is selected by outcome.

## 2. Covariant convention

Retain the AeST action convention

\[
S_g \supset -\mathcal F(\mathcal Y,\mathcal Q),
\]

with

\[
\mathcal Q=A^\mu\nabla_\mu\phi,
\qquad
\mathcal Y=(g^{\mu\nu}+A^\mu A^\nu)\nabla_\mu\phi\nabla_\nu\phi.
\]

The published conventions used by the repository are

\[
\mathcal K(Q)=-\frac12\mathcal F(0,Q)
\]

on FLRW and

\[
\mathcal J(Y)=\frac{\mathcal F(Y,Q_0)}{2-K_B}
\]

on the late-time quasistatic tracking slice.

The weak-field FLRW invariant reduction retained from B1 is

\[
Y=a^{-2}|\nabla\chi|^2+\cdots,
\qquad
\chi=\varphi+\bar Q\alpha.
\]

No ad hoc time dependence, damping, pseudo-time, or branch relaxation is introduced.

## 3. Frozen physical parameters and functions

Retain exactly

```text
K_B = 0.0665
K2  = 9500
Q0  = 1e-4 Mpc^-1
Z0  = 1e-17 Mpc^-1
a0  = 1.2e-10 m s^-2
beta0 in {1.0, 0.5, 0.1}
lambda_s = 1/beta0
```

and the same Exp homogeneous background used by the frozen CLASS model,

\[
Z(Q)=\frac{Q-Q_0}{Z_0},
\]

\[
K_{\rm Exp}(Q)=2K_2Z_0^2\left(e^{Z^2}-1\right).
\]

Retain all three already frozen full-`J` interpolation families, Simple, Exponential, and Sharp, with the unique normalization

\[
J(0)=0.
\]

Their derivatives are exactly the already frozen functions

\[
j(x)=\frac{dJ}{dY},
\qquad x=\frac{\sqrt Y}{a_0}.
\]

The Sharp branch remains piecewise-defined at its existing kink. No smoothing parameter is introduced.

## 4. Co-primary completion family

### 4.1 Separable member

Define

\[
\boxed{
F_0(Y,Q)=(2-K_B)J(Y)-2K_{\rm Exp}(Q)
}
\]

for each frozen `J`/`beta0` pair.

This is the minimal additive completion. It is not claimed to have been uniquely implied by the historical frozen model; it is an explicit D2C model-completion choice.

### 4.2 Nonseparable controls

Define

\[
G(Z)=\tanh Z,
\qquad
H(x)=\frac{x^2}{1+x^2},
\]

and freeze

```text
epsilon_mix = 0.25
sigma in {-1, 0, +1}
```

with

\[
\boxed{
F_\sigma(Y,Q)=F_0(Y,Q)
+\sigma\,\epsilon_{\rm mix}(2-K_B)\lambda_s
Y\,G[Z(Q)]\,H(x)
}.
\]

Thus `sigma=0` is exactly the separable member and `sigma=±1` are two bounded nonseparable controls.

All three values of `sigma` are co-primary. Together with the three interpolation functions and three `beta0` values, the campaign contains 27 co-primary completion/function/parameter combinations. No combination may be removed, promoted, or assigned a different gate after an output is inspected.

## 5. Why this deformation is frozen

The mixed deformation uses no new dimensionful scale:

- the `Q` dependence uses the already frozen Exp background coordinate `Z=(Q-Q0)/Z0`;
- the `Y` dependence turns on at the already frozen MOND scale `x=sqrt(Y)/a0`;
- `epsilon_mix=0.25` is a dimensionless preregistered sensitivity amplitude, not a fitted physical parameter.

It is chosen as a bounded ±25% high-gradient mixed-sector modulation. Since

\[
|G(Z)|<1,
\qquad 0\le H(x)<1,
\]

the asymptotic high-gradient coefficient generated by the control deformation remains within a factor

\[
1\pm0.25
\]

of the frozen `lambda_s` scale. This prevents the control itself from forcing a sign reversal of the high-gradient `Y` coefficient.

The purpose is not to infer `epsilon_mix`. It is to test whether the nonlinear branch conclusion is robust to a controlled covariant completion ambiguity that is invisible to the already frozen background, linear cosmology, and static tracking slice.

## 6. Exact preservation identities

Before any nonlinear FLRW trajectory is inspected, every completion must satisfy the following algebraically and numerically.

### C1. Homogeneous FLRW identity

At `Y=0`,

\[
F_\sigma(0,Q)=-2K_{\rm Exp}(Q)
\]

for every `Q` and every `sigma`, `J`, and `beta0`.

The mixed deformation starts as

\[
YH(x)=\frac{Y^2}{a_0^2+Y}=O(Y^2),
\]

so both

\[
F_\sigma(0,Q)
\]

and

\[
F_{\sigma,Y}(0,Q)
\]

are independent of `sigma`.

### C2. Frozen linear-cosmology identity

Because `Y` is quadratic in first-order spatial perturbations and the full MOND `J(Y)` contribution begins beyond the quadratic linear-cosmology action in the small-amplitude FLRW regime, the D2C mixed term must be invisible to the frozen first-order CLASS equations.

The action-derived linearization must reproduce the same Newtonian-gauge variables and equations used by the frozen CLASS bridge, including

```text
chi = Q (a theta/k^2 + alpha)
E   = alpha_dot + Psi
```

and its frozen `delta`, `theta`, `Pi`, `alpha`, and `E` evolution/constraint equations.

No `sigma`, `epsilon_mix`, `a0`, or full-`J` interpolation label may appear in the recovered first-order CLASS equations.

Required manufactured-state coefficient/residual discrepancy:

```text
<= 1e-12
```

against an independent evaluator of the already frozen CLASS equations.

### C3. Exact quasistatic tracking identity

At `Q=Q0`,

\[
G[Z(Q_0)]=0
\]

and therefore

\[
F_\sigma(Y,Q_0)=(2-K_B)J(Y)
\]

for every `sigma`.

Consequently the exact fixed-`a`, zero-time-derivative reduction must reproduce the already frozen R3/D1 equations

```text
Phi = tildePhi + chi
lap_phys(tildePhi) + mu^2 Phi = 4 pi G_N rho_b/(1+beta0)
lap_phys(tildePhi) = div_phys[j(x) grad_phys chi]
```

with manufactured-state discrepancy

```text
<= 1e-12.
```

### C4. Deep-MOND leading asymptotic

As `Y -> 0`, the mixed term is `O(Y^2)` whereas the frozen MOND contribution is `O(Y^(3/2))`. Therefore the leading deep-MOND law is unchanged.

Numerically verify for every co-primary combination that the ratio of the mixed correction to the frozen leading MOND term tends to zero on a preregistered decreasing `x` ladder.

### C5. High-gradient sign control

As `x -> infinity`,

\[
H(x)\to1.
\]

For the two nonseparable controls the mixed high-gradient modulation is bounded by `epsilon_mix=0.25`, so the corresponding multiplicative control factor remains in

```text
[0.75, 1.25]
```

for all finite `Q`.

Any implementation producing a sign reversal or leaving this bound is a FAIL, not a reason to change `epsilon_mix`.

## 7. D2C-A — action-level FLRW longitudinal derivation

For each `sigma`, derive the scalar-longitudinal weak-field FLRW equations from the covariant action before gauge reduction is used to eliminate nondynamical variables.

The derivation must identify

- lapse and scalar shift constraints;
- scalar aether variable `alpha`;
- scalar perturbation `varphi`;
- the gauge-invariant/repository combination `chi`;
- the canonical momenta or an equivalent first-order system;
- matter density and momentum source terms;
- all factors of `a`, `H`, and conformal `mathcal H`;
- the terms generated specifically by `F_sigma,Y`, `F_sigma,Q`, and the mixed derivatives.

No guessed `2H`, `3H`, damping, source projection, or static-residual evolution is allowed.

## 8. D2C-B — inherited D2B regressions

After the action-level derivation exists, repeat the inherited D2B consistency tests for all co-primary completions:

1. zero-expansion D1A regression: `<=1e-12`;
2. exact fixed-`a` quasistatic full-`J` regression: `<=1e-12`;
3. frozen linear CLASS regression over the six frozen modes: maximum normalized residual `<=2e-3` and native/dense closure `<=2e-4`;
4. D2A baryon continuity and momentum convention with no empirical correction;
5. linear constraint propagation over `0.2 <= z <= 6`: maximum normalized residual `<=2e-3` with no post-step projection.

A failure of a regression is recorded for that co-primary model and makes the conjunctive D2C PASS impossible.

## 9. D2C-C — no branch selection yet

D2C is still a formulation/completion gate. It must not inspect full-amplitude nonlinear branch outcomes to choose among `sigma=-1,0,+1`.

In particular D2C may not report

- a preferred completion;
- a preferred R3 root;
- memory survival;
- a finite-eta effect;
- an observational likelihood result.

The first full-amplitude nonlinear trajectory is a later preregistered phase only if D2C passes.

## 10. Frozen classifications

All 27 co-primary combinations satisfy C1-C5 and the complete action-derived D2C-A/B regression chain:

```text
NL1C6D2C_COVARIANT_MIXED_SECTOR_COMPLETION_PASS
```

At least one co-primary completion contradicts an exact frozen identity, develops a prohibited sign pathology under the frozen control definition, or fails an inherited regression:

```text
NL1C6D2C_COVARIANT_MIXED_SECTOR_COMPLETION_FAIL
```

The candidate family remains covariantly admissible at the exact-slice level but the required FLRW longitudinal reduction or variable mapping cannot be closed uniquely/explicitly enough to run the inherited regressions:

```text
NL1C6D2C_COVARIANT_MIXED_SECTOR_COMPLETION_INCOMPLETE
```

No gate is modified after results are inspected.

## 11. Continuation rule

Only

```text
NL1C6D2C_COVARIANT_MIXED_SECTOR_COMPLETION_PASS
```

permits preregistration of the physical nonlinear FLRW branch-evolution campaign.

That subsequent phase must run all co-primary completion members or use a completion-robust decision rule frozen before any branch output. If branch selection differs across `sigma`, the scientific result is **completion dependence**, not permission to choose the favorable model.

NL1C7 causal-memory science remains blocked until a later physical nonlinear trajectory gate passes.

## 12. Primary provenance

The completion convention follows the published AeST identities

```text
K(Q) = -F(0,Q)/2
J(Y) = F(Y,Q0)/(2-K_B)
```

and the published statement that the MOND scale `a0` enters once nonlinear terms from `F(Y,Q)` become active, while the frozen linear FLRW scalar system is expressed in the `chi`, `E`, density, momentum, and nonstandard-pressure variables already mirrored by the repository CLASS bridge.

Primary theory references:

- C. Skordis and T. Zlosnik, Phys. Rev. Lett. 127, 161302 (2021), arXiv:2007.00082.
- C. Skordis and T. Zlosnik, Phys. Rev. D 106, 104041 (2022), arXiv:2109.13287.
- M. Bataki, C. Skordis and T. Zlosnik, Phys. Rev. D 110, 044015 (2024), arXiv:2307.15126.
- P. Verwayen, C. Skordis and C. Boehm, MNRAS 531, 272-289 (2024), doi:10.1093/mnras/stae1225.
