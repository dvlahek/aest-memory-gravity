# GE15 cancellation-free s-state precision closure — implementation lock

## Status

Implementation locked before the first GE15 execution.

GE15 is an exact state-coordinate change at eta=0.

It does not modify the physical AeST equations, add a degree of freedom, alter R1/R2 values, add a finite-gradient IC correction, or relax any historical precision-closure threshold.

## Theory provenance

Immediate dependencies:

- `GE13_AEST_MODE_AMPLITUDE_ONSET_LOCALIZED`;
- `GE14_CHI_CANCELLATION_STIFF_SEED_LOCALIZED`.

GE14 established that the historical internal representation

`chi=Q(a theta/k^2+alpha)`

produces an initial cancellation residual of only a few machine-epsilon units and that the stiff `KQ chi` term dominates the initial E derivative.

## Exact state change

Define

`s = chi/Q = a theta/k^2 + alpha`.

Internally the existing legacy slot

`index_pt_alpha_aest`

is reused for `s`.

No new state index is allocated.

Physical quantities are reconstructed as

`alpha=s-a theta/k^2`

and

`chi=Q s`.

The exact transformed eta=0 equation is

`s'=a[E+Pi/(1+w)] + 3 cad2 (aH)(a theta/k^2)`.

The E equation is unchanged after substituting `chi=Qs` and `alpha=s-a theta/k^2`.

## Initial condition

The frozen leading adiabatic initial condition becomes

`s_i=0`, `E_i=0`.

No explicit finite-gradient correction is inserted in GE15.

The finite-gradient source is generated by the exact transformed `s'` equation.

## Preregistration

Commit:

`a77b560df07efa55a94e5eddbd318367aa0ae1e8`.

File:

`ge15/predata_cancellation_free_s_state_precision_closure.json`.

Frozen blob:

`18cc9e69fad27429fe91b6cb7168c087349c5b8d`.

## State-reparameterization patch

Commit:

`3fccfcd32515db59e45885ae6ca978dba775ce38`.

File:

`ge15/apply_cancellation_free_s_state_patch.py`.

Frozen blob:

`66b919b362b05721d1023441ab4a7c18f86f500c`.

The patch is applied after the existing validated AeST/memory/diagnostic trace chain.

It changes only the internal semantics of the legacy alpha slot and all required physics/diagnostic reads of that slot.

The patch source necessarily contains the old cancellation expressions as exact replacement anchors. The patched CLASS source must not contain those historical physics expressions after the patch is applied.

## Precision-closure driver

Commit:

`7eabe765e749d549094df9dbce3f4b266b722a3d`.

File:

`ge15/cancellation_free_s_state_precision_closure.py`.

Frozen blob:

`8e918742f7563dc64a1e94220dff63ac29d9e388`.

The driver reuses the frozen GE09/GE11:

- accepted-step trace parser;
- derivative-aware CubicHermiteSpline representation;
- PCHIP background/algebraic representation;
- GE06 physical local-jet dictionary;
- source-grid validation;
- every-second accepted-endpoint control.

Because GE15 diagnostic traces expose physical alpha and physical alpha-prime, the GE06 local-jet dictionary itself is unchanged.

## Frozen precision pair

R1:

- `tol_perturbations_integration=2.5e-8`;
- `perturbations_sampling_stepsize=0.00125`.

R2:

- `tol_perturbations_integration=1.25e-8`;
- `perturbations_sampling_stepsize=0.000625`.

The active pinned-CLASS parameter names remain mandatory.

## Frozen signal representation

- `k_h={0.03,0.05,0.08,0.10,0.15,0.20} h/Mpc`;
- `0.2<=z<=1.5`;
- 64 common uniform `ln(a)` nodes;
- exact same GE06 15-entry physical local jet.

## Frozen single-level gates

For both R1 and R2:

- dense trace versus official CLASS table <= `1e-12`;
- at least 16 accepted points per k in the window;
- exactly 64 common nodes;
- source-grid physical-state validation <= `1e-4`;
- primary versus every-second-endpoint global relative L2 <= `5e-4`;
- primary versus every-second-endpoint pointwise abs-or-rel <= `2e-3`;
- `p_t` identity <= `1e-12`;
- all jet entries finite.

## Frozen cross-level gates

Require:

- R1 versus R2 complete-jet global relative L2 <= `5e-4`;
- R1 versus R2 complete-jet pointwise abs-or-rel <= `2e-3`;
- per-k temporal-shape cosine for physical `alpha`, `E` and `chi` >= `0.9999`.

## Frozen representation gates

Require:

- GE15 patch report PASS;
- no state-dimension change;
- `s_i=0` exactly;
- physical alpha trace reconstructed as `s-a theta/k^2`;
- historical chi cancellation absent from the patched physics RHS/stress paths;
- active plural CLASS precision keys;
- no obsolete singular keys in the R1/R2 precision files.

## Terminal classifications

Full closure:

`GE15_CANCELLATION_FREE_S_STATE_PRECISION_CLOSURE_PASS`.

Science/control failure:

`GE15_CANCELLATION_FREE_S_STATE_PRECISION_CLOSURE_FAIL`.

Patch/provenance failure before valid science:

`GE15_CANCELLATION_FREE_S_STATE_IMPLEMENTATION_FAIL`.

## Claim boundary

A PASS certifies numerical precision closure of the eta=0 first-order AeST local jet under an exact state reparameterization.

It does not:

- relabel historical GE09, GE11 or GE11 Repair01;
- close the frozen GE08 Repair01 standard-matter incompleteness;
- solve `Z20` or `Z21`;
- introduce finite eta;
- establish a collapse or observational result.

## Anti-tuning

After this lock do not:

- change R1/R2 numerical values;
- add R3;
- add an explicit finite-gradient IC correction;
- change the signal window;
- change interpolation families;
- change the GE06 physical jet dictionary;
- relax any frozen single-level or cross-level threshold.
