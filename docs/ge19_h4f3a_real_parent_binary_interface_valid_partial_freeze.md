# GE19 H4F3a — real H3F/H3G binary parent interface, partial PASS

## Classification and exact boundary

`GE19_H4F3A_H3F_H3G_BINARY_INTERFACE_PASS_Z11_REQUIRED`.

The actual, user-supplied H3F and H3G binary results
were inspected with an independently executed, separately
versioned script. Both hashes and certified JSON classifications
matched the already frozen H4F3 preregistration.

This is an **actual parent input-interface result**, not a
synthetic fixture. It establishes an exactly common
H3F/H3G time grid and their exact stored q20-to-B20
algebraic relation for all original C and beta cohorts.

The certified Repair32B Z11 NPZ was **not present in the
available mounted uploads**. This was recorded explicitly;
the all-three-parent interface remains incomplete.
The result does not evaluate the actual H4 source families,
full operator Ward term, dust/bath parent Euler residuals,
common physical H(t) or Z21.

## Frozen source and actual output provenance

Existing H4F3 preregistration:
`ge19/h4f3_predata_integrated_corrected_parent_ward.json`;
blob `3e17163cb6f52f3a78c41e6f37f83ef682359162`.

New actual parent auditor:
`ge19/h4f3a_actual_parent_array_interface_audit.py`;
blob `0b4fb3ca120f54fef3b53aab452511d374062b68`;
7491 bytes. Its Git blob matches byte-for-byte the exact
locally executed auditor.

Versioned true-result report:
`results/ge19_h4f3a_actual_parent_array_interface.json`;
blob `0e7fa9969e9c46490bf81b0073b4d4fcfcb41ae1`;
2695 bytes; SHA-256
`77777bf942e771c3f22dff7f43651b6f944af58790e0353e5919c9972fd7a4c9`.

The following four uploaded parent files were checked
against their exact immutable SHA-256:

- H3F JSON:
  `0616188d2bb7a6c09b2b56433a1f8a1860f360b2e54d2cb84e1ae214a407866b`;
- H3F NPZ:
  `90840755fa9febb1d8cb84609d9e58f67dec2a0a01cd6bf8e47685b45caa4542`;
- H3G JSON:
  `9b93534e3ee90e1ce588bdbd3f271afd041f738b8dc6f62c4ec0d1413c27f2c4`;
- H3G NPZ:
  `9e1bf36e1d81122225ff8c03f663501de7601a8fc9376fd86312e0ae1d809452`.

Both source classifications and their original
science gates were checked, without modifying
any H3F/H3G outputs.

## Actual verified interface

`x_primary` is exactly identical, 128 time nodes,
and `x_control` exactly identical, 64 time nodes,
for H3F and H3G. Both grids run from
`-0.916290731874155` to
`-0.1823215567939546` in `x=ln(a)`.

For every `C_min,C_star,C_max`:

- H3F primary Z20: `(3,40,6,128)`;
- H3F primary on-shell H1: `(6,6,128)`;
- H3G weighted q20, X20 and B20:
  `(3,40,128)`;
- their Nt64 controls have the expected
  corresponding lengths;
- all inspected parent arrays are finite;
- primary weighted q20 is exactly zero
  at the first time node;
- the **stored** identity
  `B20_linear=X20-weighted_z20`
  holds elementwise with maximum absolute
  difference `0.0` for each C cohort.

The exact stored B20 identity does not
independently re-derive X20 from the
covariant action. H3G's original science
certification and source-binding controls
remain separate parents for that claim.

## Actions static validation (not a second binary run)

Static workflow:
`.github/workflows/ge19-h4f3a-parent-interface-static.yml`.

Run `36056344500`, job `107824177598`,
conclusion `success`, marker
`GE19_H4F3A_REAL_PARENT_ATTESTATION_STATIC_PASS`.

Actions checked the exact committed source and
result Git blobs, compiled the script, tested
its CLI contract, and compared the attested
four real parent hashes with the frozen
H4F3 preregistration. It explicitly checked
`Z11.provided=false`, full-Ward flags false
and `Z21_certified=false`.

**Actions did not download or re-read the
four real binary parent files.** Their
true-data checks were performed by the
locally executed script on the available
uploaded binaries, and the exact report
was then committed to the repository.

## Next data/computation boundary

The script has an optional `--z11-npz`
argument. The separately certified
Repair32B NPZ must match SHA-256

`5d4a0a72c08d09d096a8de0b428b3c8443fc33e8ad442ed6d997d6bf2bc6e327`.

When it is supplied, the script requires
both original H3F time grids to match
the Z11 time grids exactly and checks
Z11 and Z11dot shapes/finite values in
all C cohorts. A PASS would complete
**only the three-parent binary interface**,
not the full integrated H4F3 Noether test.

After Z11 binding, separately implement
and execute one complete physical-clock
H4F3 six-piece source+independent operator+
signed all-parent Euler Ward evaluation.
The frozen H4F2h FD8/FD4 treatment and
unchanged original active shift `1e-6`
target remain mandatory. No inferred
H4/Z21 or lensing result.
