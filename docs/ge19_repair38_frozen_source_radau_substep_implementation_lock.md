# GE19 Repair38 frozen-source Radau substep localization — implementation lock

## Status

Repair38 is frozen before its first diagnostic execution.

Repair38 is diagnostic only. It cannot relabel Repair37, certify Z21, license
lensing, change the H4 equation or relax the frozen `1e-6` shift target.

Its sole purpose is to test if the remaining Repair37 propagated-shift floor
is generated by the frozen two-stage Radau IIA march.

## Frozen Repair37 valid FAIL

Localization freeze:

`docs/ge19_repair37_valid_fail_propagation_order_localization_freeze.md`.

Freeze commit:

`8ef52885d33aac1f91bc75261d53ce8b3b4c54dc`.

Freeze blob:

`d120915221b5fc1b0860c682cb8edac876571e83`.

Frozen artifacts:

- JSON/FULL SHA-256:
  `da8f2f00c22c866ec3f82381d23f69bf036e630fe2a29c5c44657984b760f61a`;
- NPZ SHA-256:
  `572d8937c1d742b10da66e34cc076377c1b2feb20b8f72eb25c3eaf31a59829f`;
- outer runner SHA-256:
  `e3ca1f3c049b45b320c5cdd7db752fa790ed5969de1f9ef350eed0ca72e64522`.

Repair37 remains historical FAIL.

## Frozen preregistration

File:

`ge19/repair38_predata_frozen_source_radau_substep_localization.json`.

Final preregistration commit:

`a3c5ac84c7a0769f91f26fd3ead17445549bc964`.

Blob:

`41a2925cf4f705c4bf8418cbcc2cee48af4a673d`.

## Frozen implementation

File:

`ge19/repair38_frozen_source_radau_substep_localization.py`.

Implementation commit:

`7ac789069eda7af0f86ff8a7003428cc5c4d0c24`.

Blob:

`fd0c80f73f0e1b485ecb895566bef078a12ba957`.

The implementation loads, rather than reconstructs:

- Repair37 Nt128 total H4 `main` source arrays;
- Repair37 Nt128 total H4 constraint-source arrays;
- Repair37 projected `p0`;
- Repair37 primary shift-scale arrays defining the frozen active mask.

No GE06/GE07 Q source, DY2 source, memory M1/M2 source or Lambda mixed source
is recomputed in Repair38.

## Dedicated prelock

Workflow:

`.github/workflows/ge19-repair38-prelock-audit.yml`.

Workflow commit:

`07606e314310d77cad1d04a0234997be1dc10f3a`.

Workflow blob:

`dd88bc6ef75003b927869b4e900e7071368b2d23`.

Run:

`35839113490`.

Job:

`107109657750`.

Conclusion:

`success`.

The prelock verifies:

- Repair37 remains frozen and not relabelled;
- the emitted Repair37 JSON/NPZ SHA-256 values are hard-bound;
- Repair38 is diagnostic-only;
- the H4 source is not rebuilt;
- frozen projected `p0` is loaded rather than resolved;
- the source-stage representation remains Repair07 PCHIP;
- the shift metric remains Repair07 cancellation-safe backward error;
- the Radau tableau is unchanged;
- only internal substep factors `1,2,4` vary;
- the frozen science target remains `1e-6`;
- no observational input or lensing claim is present.

## Factor-1 reproduction rule

Before interpreting finer propagation, factor 1 must reproduce the frozen
Repair37 Nt128 result:

- global Z21 relative L2 <= `1e-11`;
- global shift-metric relative L2 <= `1e-10`;
- projected `p0` exactly identical;
- frozen active sample count exactly `23850`.

A factor-1 reproduction failure is an implementation failure, not a
propagation result.

## Diagnostic routing

`RADAU_PROPAGATION_FLOOR_CONFIRMED` requires:

- factor 2 / factor 1 active Linf <= `0.8`;
- factor1-to-factor2 observed Linf order >= `2.5`;
- factor 2 active Linf <= `1e-6`;
- factor 4 active Linf <= `1e-6`.

This route does not certify Z21. It licenses only a separately preregistered
higher-accuracy H4 reclosure.

If substepping improves the result materially but does not satisfy the strict
confirmation rule, the route is
`PROPAGATION_ACCURACY_DEPENDENCE_CONFIRMED`.

If substepping does not materially remove the floor, the route is
`PCHIP_OR_OTHER_FLOOR_REMAINS`.

## Claim boundary

Repair38 cannot certify a primordial homogeneous Z21 mode, a full-species
nonlinear cosmology, finite eta, lensing or any observational signal.

Repair38 cannot alter the Repair37 classification.

It is a numerical localization test only.
