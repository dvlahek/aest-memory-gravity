# NL1C6D2C6F pre-data — finite-eta direct gravitational source on the certified nonlinear memory trajectories

## Status and parent chain

This declaration is frozen before any NL1C6D2C6F numerical source result is inspected.

Scientific parent: `NL1C6D2C6E_LARGER_ETA_RETAINED_SCALAR_CURRENT_PASS`, computed at head `e51748d58c9cb43340e2ad583c5985641fa9aad6` and recorded in `docs/nl1c6d2c6e_larger_eta_result.md` at commit `dd49c48fb540109610bbfcb417701f97ee98554b`.

Theory parents are the already frozen covariant memory completion and component reductions:

- `NL0B_COVARIANT_MEMORY_COMPLETION_PASS`;
- `NL1B2_DIRECTIONAL_SECOND_ORDER_ETA_TANGENT_PASS`;
- `NL1C3A_COMPONENT_VARIATIONAL_GENERATOR_PASS`;
- `NL1C3B_LONGITUDINAL_3PLUS1_MEMORY_BRIDGE_PASS`;
- `NL1C6D2C5_ACTION_LEVEL_FLRW_LONGITUDINAL_REDUCTION_PASS`.

The historical `NL1B_SECOND_ORDER_DYNAMICAL_CLOSURE_INCOMPLETE` classification remains unchanged. D2C6F is deliberately narrower than a full component-expanded arbitrary-amplitude relativistic FLRW evolution.

## Scientific question

D2C6C--E established a stable finite-positive-eta nonlinear scalar-current response while the external CLASS metric/matter history was frozen. D2C6F asks the next action-level physics question:

> On those same certified finite-eta trajectories, what direct lapse, shift, isotropic spatial and longitudinal-shear gravitational source is generated by the frozen NL0B memory action, and is that source numerically controlled across the nonlinear-completion ensemble?

This is a one-way gravitational-source certification. It does **not** feed the new direct memory metric source back into the scalar-current, metric or matter trajectory. A self-consistent feedback stage is allowed only after D2C6F passes.

## Frozen action dictionary

Use the exact longitudinal 3+1 NL1C3B reduction, node by node,

\[
\widehat{\mathcal L}_j=\frac{NLR^2}{4}\left[(Aq_j)^2-(\omega_jq_j-\sqrt{w_j}X)^2\right],
\]

with the physical outer factor `eta` inherited from `U_j=sqrt(eta) q_j`.

Evaluate the metric variation on the FLRW/Newtonian reference

\[
N=1,\qquad L=R=a,\qquad b=r=0,
\]

with `X=(1/a) partial_x chi`. The D2C6C--E bath variable is the algebraic bridge variable

\[
z_j=\frac{\omega_j q_j}{k\sqrt{w_j}},
\]

so the action variable is reconstructed spectrally as

\[
q_j=\frac{\sqrt{w_j}}{\omega_j}\sqrt{-\nabla^2}\,z_j,
\qquad
q'_j=\frac{\sqrt{w_j}}{\omega_j}\sqrt{-\nabla^2}\,z'_j,
\qquad
\dot q_j=q'_j/a.
\]

No new physical coefficient, stress ansatz, Poisson replacement or interpolation function is permitted.

The direct metric Euler-Lagrange blocks are generated from the same action. On the frozen FLRW reference they are equivalently

\[
S_N=-\frac{a^3}{4}\sum_j\left[\dot q_j^2+\left(\omega_jq_j-\sqrt{w_j}\frac{\partial_x\chi}{a}\right)^2\right],
\]

\[
S_b=-\frac{a^3}{2}\sum_j \dot q_j\,\partial_x q_j,
\]

\[
S_L=\frac{a^2}{4}\sum_j\left[\dot q_j^2-\omega_j^2q_j^2+w_j\frac{(\partial_x\chi)^2}{a^2}\right],
\]

\[
S_R=\frac{a^2}{2}\sum_j\left[\dot q_j^2-\left(\omega_jq_j-\sqrt{w_j}\frac{\partial_x\chi}{a}\right)^2\right].
\]

All four physical source blocks are multiplied by `eta`. `S_R` is the variation with respect to the common transverse scale `R`, hence already contains both transverse directions.

For reporting only, define the exact Newtonian-gauge variation combinations

\[
S_\Psi=S_N,\qquad S_\Phi=-a(S_L+S_R),\qquad
S_{\rm shear}=S_L-S_R/2,
\]

plus the shift block `S_b`. These are action-source combinations; D2C6F will not relabel them as an observational density, pressure or lensing signal.

## Frozen ensemble and amplitudes

Use exactly the D2C6E completion family:

- `sigma in {-1,0,+1}`;
- `kind in {simple, exponential, sharp}`;
- `beta0 in {1,0.5,0.1}`;
- `epsilon_mix=0.25`;
- 27 members total.

Use exactly the already certified D2C6E physical amplitudes

\[
\eta\in\{1/32,1/16,1/8\}=\{0.03125,0.0625,0.125\}.
\]

No larger eta value is introduced in D2C6F.

Use the same six seeded modes/phases, CLASS background, `K_B=0.0665`, corrected Exp branch, `tau H0=1`, redshift checkpoints, periodic box and modewise finite-eta prehistory as D2C6E.

Primary resolution: bath order 39, `Nx=128`, `Nstep=4096`.

At `eta=0.125`, numerical controls are frozen as:

- bath order: order 47, `Nx=128`, `Nstep=4096`;
- time: order 39, `Nx=128`, `Nstep=8192`;
- space: order 39, `Nx=256`, `Nstep=4096`.

## Frozen gates

### F1 — provenance and exact coverage

The D2C6E computational parent and this preregistration must be ancestors of the run head. Exactly `27 x 3 = 81` primary source trajectories must be present.

### F2 — action-source implementation audit

Before the science ensemble, the implemented `S_N,S_b,S_L,S_R` evaluator must agree with independent central finite differences of the discrete NL1C3B action with respect to lapse, shift, longitudinal scale and common transverse scale on deterministic nonzero fields.

Gate: maximum relative discrepancy `<= 1e-6`.

The spectral `z -> q` bridge must reproduce the frozen modewise algebraic identity to relative L2 `<= 1e-10` on deterministic supported Fourier modes.

### F3 — eta-zero and completed-square structural controls

Because the physical direct metric source carries the explicit outer `eta`, its eta-zero source must vanish exactly to stored precision.

The lapse-energy identity

\[
-S_N/ a^3=\frac14\sum_j\left[\dot q_j^2+(\omega_jq_j-\sqrt{w_j}X)^2\right]\ge0
\]

must hold pointwise up to normalized numerical tolerance `1e-12` for all primary and control trajectories. This is an algebraic action identity, not a phenomenological sign gate.

### F4 — parent finite-eta trajectory regression

For one deterministic anchor member (`sigma=-1, kind=simple, beta0=1`) at `eta=0.125`, the D2C6F trajectory implementation must reproduce the frozen D2C6D/E retained scalar-current state and E history to relative L2 `<= 1e-10`. D2C6F may record extra bath/source state only; it may not alter the parent dynamics.

### F5 — primary health

All 81 primary trajectories must be finite, retain the D2C6E scalar constraint residual `<=1e-10`, and satisfy `min(1+j_eff)>0`.

All direct metric source fields and reporting combinations must be finite.

### F6 — bath-order convergence of the gravitational source

At `eta=0.125`, compare order 39 against order 47 using normalized Fourier source signatures over the stored redshift checkpoints. Gate: worst relative L2 across `S_Psi,S_Phi,S_b,S_shear` and all 27 members `<=1e-2`.

### F7 — time convergence of the gravitational source

At `eta=0.125`, compare `Nstep=4096` against `8192`. Gate: worst relative L2 `<=2e-3`.

### F8 — space convergence of the gravitational source

At `eta=0.125`, compare `Nx=128` against `256` using the common normalized Fourier modes. Gate: worst relative L2 `<=5e-3`.

### F9 — scope

No feedback of `S_N,S_b,S_L,S_R` into the metric/matter/scalar-current dynamics, no new eta amplitude, no branch selection, no likelihood and no observational data are allowed in D2C6F.

## Non-gating physics diagnostics

The following are reported but **must not** alter PASS/FAIL:

- source RMS and low-mode spectra versus redshift for `S_Psi,S_Phi,S_b,S_shear`;
- `S/eta` dyadic quotient changes between the three eta values;
- completion-family spread at fixed eta/redshift;
- which source block/member/redshift is largest;
- direct-source anisotropy/shear fraction;
- whether the source remains approximately eta-linear or develops visible finite-eta curvature.

No sign, minimum amplitude, detection significance, preferred completion, preferred eta or source ordering is preregistered.

## Classification

PASS iff F1--F9 all pass:

`NL1C6D2C6F_FINITE_ETA_DIRECT_GRAVITATIONAL_SOURCE_PASS`

Otherwise:

`NL1C6D2C6F_FINITE_ETA_DIRECT_GRAVITATIONAL_SOURCE_FAIL`

A technical inability to complete the frozen calculation without obtaining the required output is recorded as:

`NL1C6D2C6F_FINITE_ETA_DIRECT_GRAVITATIONAL_SOURCE_INCOMPLETE`

PASS licenses a separately preregistered self-consistent longitudinal weak-field metric/matter feedback step. It does **not** license a claim of full arbitrary-amplitude nonlinear GR/AeST, a Planck/ACT/SPT fit, or an observational detection.