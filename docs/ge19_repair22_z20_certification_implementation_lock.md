# GE19 Repair22 on-shell-parent Z20 certification lock

## Status

**SCIENCE CERTIFICATION IMPLEMENTATION LOCKED BEFORE REPAIR22 LOCAL RESULT**

Repair21 is frozen with route

`INTERPOLATED_H1_PARENT_DEFECT_CONFIRMED`.

Repair22 is the first explicit Z20 certification rerun after that diagnostic closure.

## Parent Repair21 freeze

Commit:

`a6fea1e32aebef65e43151030fe90fc6263d6ce0`

File:

`docs/ge19_repair21_on_shell_h1_matched_shift_result_freeze.md`

Blob:

`a5deb856a60c8df1c921110dc255e379806087e5`

Frozen Repair21 artifacts:

- JSON SHA-256:
  `e27d12f18a992a1c8c3217e67c0efd39dcf7c9d7aadcfbbb3220f7508bca1bb2`;
- NPZ SHA-256:
  `c6fcde7d39480ec7f03de2acf0f33f648a997404c9cca8f83990ed7b50667f3b`.

## Repair22 preregistration

Commit:

`515a6ac728d056330d33f4e266a53f5693fc6548`

File:

`ge19/repair22_predata_on_shell_parent_z20_certification.json`

Blob:

`a3d34b23acedf071b4331c0ca27be85d3e7f5e4b`

## Repair22 implementation

Commit:

`f7aef2aa9ca8a34bd2ffe0e53a7a230f461a1926`

File:

`ge19/repair22_on_shell_parent_z20_certification.py`

Blob:

`4860d13c881f41dbacbdc16ff179e917644ea6a5`

## Exact certification construction

Primary:

- Nt=128;
- Nx=1024.

Control:

- Nt=64.

Source spatial control:

- Nx=2048 on the genuine Nt128 on-shell H1 parent.

The certification run recomputes the locked Repair21 on-shell-H1/H3 core rather than certifying the stored Repair21 arrays post hoc.

## Frozen physics

Unchanged:

- Repair13 self-consistent reduced background;
- Repair07 canonical H1 and H3 operators;
- Repair11 Lambda c1;
- Repair14 GE06+GE07+Lambda c2+Y2 H3 source;
- Repair18 q0=0 projected-p0 boundary;
- Repair07 two-stage Radau IIA canonical propagation;
- C envelope;
- beta0={1,0.5,0.1};
- m=1..40.

No source formula, sign, component order or normalization is changed.

## Frozen H1 certification gates

- Nt64 frozen Z10 reproduction <= `1e-12`;
- Nt64 frozen Z10dot reproduction <= `1e-12`;
- H1 linear residual <= `1e-8`;
- H1 shift <= `1e-6`;
- H1 anisotropy <= `1e-6`;
- initial dynamic match <= `1e-10`;
- Nt128/Nt64 H1 state relative L2 <= `5e-3`;
- finite outputs.

## Frozen source-spatial gate

On the Nt128 on-shell parent, the exact Repair14 H3 source is evaluated at:

- Nx1024;
- Nx2048.

The original Repair14 low-mode m=1..40 relative-L2 criterion is retained:

`<= 5e-4`.

## Frozen boundary gates

For all Nt128/Nt64 C,beta,m cases:

- projected p0 relative L2 vs frozen Repair18 <= `1e-12`;
- scaled constraint residual <= `1e-8`;
- lapse backward error <= `1e-6`;
- shift backward error <= `1e-6`;
- eliminated algebraic residual <= `1e-8`;
- coefficient rank = augmented rank = 2.

## Frozen shift certification

The original Repair07 all-row relative metric is still computed and reported.

It is **not** the certification gate because Repair20/21 established that its O(1) maximum is generated by structurally near-null rows where both residual and natural scale are machine-level.

Primary Nt128 defines:

`S_ref = max natural shift scale`.

Near-null:

`scale <= sqrt(eps_float64) S_ref`.

Active:

`scale > sqrt(eps_float64) S_ref`.

Certification requires:

- active Nt128 shift Linf <= original `1e-6`;
- near-null max absolute residual / S_ref <= `1000 eps_float64`;
- matched Nt64->Nt128 active Linf order >= `2.5`;
- matched Nt64->Nt128 active RMS/L2 order >= `2.5`.

This is a monitor-domain correction after the independently frozen Repair20/21 diagnosis. The active tolerance is not relaxed.

## Other H3 gates

- H3 linear residual <= `1e-8`;
- anisotropy <= `1e-6`;
- Nt64/Nt128 state relative L2 <= `5e-3`;
- all C,beta,m cases complete;
- all source and state outputs finite.

## Frozen routing

Only:

- `GE19_REPAIR22_ON_SHELL_PARENT_Z20_CERTIFICATION_PASS`;
- `GE19_REPAIR22_ON_SHELL_PARENT_Z20_CERTIFICATION_FAIL`;
- `GE19_REPAIR22_ON_SHELL_PARENT_Z20_CERTIFICATION_IMPLEMENTATION_FAIL`.

A PASS sets

`Z20_certified=true`

only for the low-mode constraint-certified window-local reduced-H3 particular directional state.

## Dedicated prelock

Workflow:

`.github/workflows/ge19-repair22-prelock-audit.yml`

Blob:

`e39a27ccfa78a6d4c03dcefafd14bdd9284f2b0c`

Run:

`35636884016`

Job:

`106456372063`

Conclusion:

`success`.

Terminal marker:

`GE19_REPAIR22_PRELOCK_AUDIT_PASS`.

Frozen implementation blobs reproduced:

- Repair07:
  `e34d28a2062c748f48bc82fa928844b02631de25`;
- Repair13:
  `362d63d03d7b850fceae393f535353ded79aeea7`;
- Repair14:
  `06c5ced952c2370cfa4aaadb6ef8f72d2d7221de`;
- Repair18:
  `c7b3a5c689bd78a65a8150c26e1fbfae108cb580`;
- Repair21:
  `73e7fa0f5b3a58f0462308bf11d2a53b7616abfb`.

## Next step after PASS

Do not start H4/Z21 directly.

First freeze Repair22 PASS, then construct q20 in a separately preregistered step.

Only after q20 is frozen may H4/Z21 be licensed.

## Claim boundary

Repair22 can certify only the low-mode constraint-certified window-local reduced-H3 Z20 particular directional state on the Repair13 self-consistent background.

It does not certify:

- primordial/homogeneous second-order content;
- full species;
- finite eta;
- physical-amplitude nonlinear evolution;
- lensing;
- observational inference.
