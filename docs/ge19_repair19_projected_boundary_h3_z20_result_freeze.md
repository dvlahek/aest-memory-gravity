# GE19 Repair19 projected-boundary reduced-H3 Z20 propagation result freeze

## Status

Frozen first locked Repair19 local science execution.

Terminal classification:

`GE19_REPAIR19_PROJECTED_BOUNDARY_REDUCED_H3_Z20_PROPAGATION_FAIL`.

This is a historical science FAIL under the preregistered Stage-B gates.

Repair18 remains a certified boundary PASS.

Repair19 does **not** certify Z20.

## Frozen local outputs

Science JSON:

- bytes: `2613226`;
- SHA-256: `32837e04a9ea6c83642a0d465f0312cc17a02ddad168760764f3c7b999a0a14d`.

Inner FULL log:

- bytes: `2613226`;
- SHA-256: `32837e04a9ea6c83642a0d465f0312cc17a02ddad168760764f3c7b999a0a14d`.

Science NPZ:

- bytes: `9505678`;
- SHA-256: `d638c86e48dd5c912629068f56c3780d0a611ed26ff5c161cd792a14328f7215`.

Outer local runner log:

- bytes: `2616791`;
- SHA-256: `6bdff0aaee50f242b51df735acb2a6bc1277bfb5aa8231dbc60c94704fb19d35`.

## Provenance

Repair13 frozen parent exact.

Repair18 frozen boundary exact.

Repair14 source construction reused without physics edit.

Repair18 projected p0 reproduction:

- max relative L2 mismatch vs frozen Repair18: `0.0`;
- primary/control projected p0 relative L2 mismatch: `0.0`.

Initial projected-boundary controls:

- scaled constraint residual max: `2.482534153108436e-16`;
- lapse backward error max: `2.639993079262776e-16`;
- shift backward error max: `3.2234628832120975e-16`;
- anisotropy backward error max: `6.394777335818628e-16`;
- eliminated algebraic residual max: `2.457039030496151e-16`;
- rank 2 throughout.

Thus the Repair18 boundary is reproduced exactly and is not the source of the Repair19 FAIL.

## Unchanged source and convergence controls

Source spatial convergence:

`1.1166216258507802e-12`

against threshold `5e-4`.

Primary linear-system residual:

`1.2568088493786592e-13`

against threshold `1e-8`.

Primary anisotropy residual:

`5.176430012533252e-16`

against threshold `1e-6`.

Nt64/Nt32 propagated-state relative L2:

`3.77673237391757e-4`

against threshold `5e-3`.

All sources and propagated outputs are finite.

## Failed gate

Primary shift constraint backward-error max:

`1.8946212213942455`

against threshold `1e-6`.

Control Nt32 shift max:

`1.9278909582169974`.

This is the only frozen science gate that fails.

## Important numerical structure of the shift FAIL

The largest O(1) relative shift metrics occur on near-null rows.

Examples:

- C_min, m=9:
  relative metric `1.8946212213942455`,
  absolute residual `2.4711147006567223e-18`,
  row scale `4.715763751872156e-18`;
- C_min, m=4:
  relative metric `1.7732519270453524`,
  absolute residual `1.7171373850996022e-18`,
  row scale `1.9372227078600296e-18`.

The O(1) number is therefore generated by dividing a machine-level residual by a machine-level constraint scale.

Across primary modes the shift-row scales are strongly bimodal:

- near-null maximum observed scale:
  approximately `1.67e-16`;
- active minimum observed scale:
  approximately `1.84e-8`.

There is therefore a large numerical gap between structurally near-null and active shift rows.

## Active-row behavior

On active shift rows the relative residual is not O(1).

Representative primary values:

- m=2:
  shift `7.367439792395178e-6`;
- m=3:
  shift `7.397521660196692e-6`;
- m=5:
  shift `7.356420874633711e-6`.

For m=5:

- absolute shift residual `2.337967262620318e-9`;
- row scale `5.418590605268847e-4`.

Thus the active-row failure is small but above the frozen `1e-6` threshold.

## Interpretation boundary

Repair19 remains a historical FAIL.

However, the evidence does **not** support interpreting it as a new source, boundary or algebraic-operator failure:

- Repair18 boundary is reproduced exactly;
- source convergence is excellent;
- canonical linear-system residual is near machine precision;
- anisotropy is near machine precision;
- Nt64/Nt32 state convergence passes comfortably;
- only the shift monitor fails.

The O(1) maxima are dominated by near-null normalization.

The remaining active-row shift residual is consistent with a time-discretization truncation hypothesis and requires a preregistered resolution ladder before any monitor or threshold change.

## Next licensed diagnostic

Repair20 may perform a frozen 32/64/128 time-resolution ladder using the same Repair13/Repair14/Repair18 physics and boundary.

Repair20 must:

- preserve the original shift threshold `1e-6`;
- report both absolute residual and natural row scale;
- separate near-null rows using a preregistered scale-relative machine-precision rule;
- estimate observed convergence order on active rows;
- test the original shift gate at Nt128 without changing its definition;
- make no source, boundary or physics edit.

No Repair19 relabeling is permitted.

## Stop rule

No q20 and no H4/Z21.

No observable/data bridge.

No shift-threshold relaxation.

## Claim boundary

Repair19 certifies only that the projected-boundary H3 march is numerically stable and converged in every frozen control except the shift gate. Z20 remains uncertified.
