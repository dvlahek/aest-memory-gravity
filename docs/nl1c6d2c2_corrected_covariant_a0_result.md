# NL1C6D2C2 corrected covariant A0 structural result

## Classification

`NL1C6D2C2_CORRECTED_COVARIANT_A0_STRUCTURAL_PASS`

Successful GitHub Actions run: `34437397304`.
Branch: `v053-exp-normalization-corrected`.
Run head: `20bc491e6dac415b113125546d8d33cf502086ad`.

The immediately preceding run `34437318387` produced the same scientific PASS values but was technically red because `results/` was not created before `tee`. The only remediation was `mkdir -p results` in the shell runner; no physics code, equation, parameter, tolerance, or gate changed.

## Frozen A0 gate results

- A0.1 corrected Minkowski curvature: PASS. `K_QQ(Q0)=19000=2 K2` and `mu^2=9.826739074217741e-05 Mpc^-2` exactly reproduce the corrected/D1A normalization.
- A0.2 D1A canonical regression: PASS, maximum relative error `1.522270386341e-16`.
- A0.3 fixed-a full-J operator regression: PASS, maximum relative error `1.474780063071e-15`.
- A0.4 completion exact slices: PASS, maximum discrepancy `0`.
- A0.5 published-linear to CLASS-convention manufactured regression: PASS, maximum relative discrepancy `4.629127056437e-16` over 64 deterministic manufactured states.
- A0.6 corrected source-anchor provenance: PASS.
- A0.7 scope: PASS. No nonlinear FLRW trajectory, branch selection, memory forcing, finite eta, likelihood, refit, or R3 solver change was performed.

## Interpretation

The factor-2 normalization contradiction that caused the historical D2C hard FAIL is absent on the corrected model revision. The action variables, corrected local curvature, D1A canonical limit, fixed-a full-J operator, completion slices, and published linear FLRW-to-CLASS variable/time conversion are mutually consistent at the preregistered precision.

This PASS licenses only D2C2-A1: derivation/audit of nonlinear FLRW terms generated by `F_sigma,Y`, `F_sigma,Q`, and mixed derivatives. It does not license physical nonlinear branch evolution or NL1C7.
