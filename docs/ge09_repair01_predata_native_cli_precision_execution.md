# GE09 Repair01 predata — native CLASS CLI precision execution

## Status

Repair preregistration after

`GE09_PRE_SCIENCE_IMPLEMENTATION_FAIL`.

No GE09 local-jet science gate has yet been evaluated.

## Parent implementation failure

Pinned `classy` rejects precision-file keys passed through `Class.set()`.

The frozen p3 values must therefore be supplied through their native CLASS
precision-file path.

## Frozen Repair01 change

Replace only the execution surface.

Run the unchanged patched CLASS executable as

`class ge09.ini v019p/pre/p3.pre`.

The p3 file remains unchanged:

- `tol_perturb_integration = 5e-8`;
- `perturb_sampling_stepsize = 0.0025`.

The same environment variables produce:

- the GE09 successful-step state/RHS trace;
- the unchanged GE08 accepted-source state trace.

## Independent CLASS-state control

The parent preregistration required an independent check that the diagnostic
successful-step trace reproduces standard CLASS perturbation output.

Under the native CLI execution, use the official files generated by
`k_output_values`:

`perturbations_k0_s.dat ... perturbations_k5_s.dat`.

Compare the GE09 dense trace against the standard columns:

- tau;
- a;
- phi;
- psi;
- delta_cdm;
- theta_cdm.

The frozen abs-or-rel gate remains exactly `1e-12`.

This replaces only the wrapper-specific `get_perturbations()` surface. It
does not remove or relax the independent CLASS-state control.

## Everything else unchanged

Keep exactly:

- six k modes;
- `0.2<=z<=1.5`;
- 64 common ln(a) nodes;
- full accepted-step primary representation;
- deterministic every-second endpoint control;
- CubicHermiteSpline/PCHIP split;
- source-grid state validation <= `1e-4`;
- primary/control global relative L2 <= `5e-4`;
- primary/control pointwise abs-or-rel <= `2e-3`;
- scalar pt identity <= `1e-12`;
- complete 15-entry GE06 local-jet dictionary;
- all claim boundaries.

## Classification

A completed science execution still uses only:

- `GE09_DENSE_ACCEPTED_STEP_LOCAL_JET_BRIDGE_PASS`;
- `GE09_DENSE_ACCEPTED_STEP_LOCAL_JET_BRIDGE_FAIL`.

An execution/runtime failure before science output remains an implementation
failure and does not consume the science classification.
