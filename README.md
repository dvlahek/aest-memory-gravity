# AeST Memory Gravity

[![v018 CLASS compile and zero regression](https://github.com/dvlahek/aest-memory-gravity/actions/workflows/v018-class-compile.yml/badge.svg)](https://github.com/dvlahek/aest-memory-gravity/actions/workflows/v018-class-compile.yml)
[![v019n strict structural null](https://github.com/dvlahek/aest-memory-gravity/actions/workflows/v019n-structural-null.yml/badge.svg)](https://github.com/dvlahek/aest-memory-gravity/actions/workflows/v019n-structural-null.yml)

Research code and validation harness for an AeST-based history-dependent gravitational memory model.

## Current status

**v0.18 — CLASS source integration and zero-regression: PASS.**

The workflow pins official CLASS v3.3.4 at commit `e85808324f51fc694d12e3ed7439552a3c3f9540`. All nine generated CLASS `.dat` outputs are byte-identical between pristine and patched builds when the extension is inactive.

**v0.19 — eta=0 AeST CLASS bridge: diagnostic PASS.**

The published Cosh/Exp background families and scalar states `delta_A`, `theta_A`, `alpha_A`, `E_A` compile and run in CLASS with memory disabled. The current radiation-era scalar initial condition is the regular proxy `chi_i=0, E_i=0`, not a claim to reproduce the unpublished exact AeST adiabatic initial-condition derivation.

**v0.19p — precision/quasi-null test: PASS.**

For the Exp quasi-null background control, the primary TT/EE/TE peak-normalized residual relative to matched CDM is

`6.855294693020108e-4`

at both standard precision and the strictest tested precision (`tol_perturb_integration=5e-8`, `perturb_sampling_stepsize=0.0025`). CDM and Exp p0-to-p3 self changes are zero at written-output precision. Thus ordinary CLASS perturbation tolerance does not explain the residual.

**v0.19n — strict structural-null extra-state test: PASS.**

A test-only mode keeps the standard CDM background, stress-energy and `delta/theta` dynamics while allocating the same two extra scalar states `alpha_A`, `E_A`, initialized to zero and frozen with zero derivatives. Standard CDM and this frozen-extra-state control are numerically identical at both p0 and p3 across the compared CMB spectra, matter power, background and thermodynamics outputs.

Therefore the persistent v0.19p Exp–CDM residual is not caused by ODE-vector dimension or adaptive error normalization. Within the present bridge it is generated by the active eta=0 AeST proxy perturbation dynamics.

## Next gate

Derive and implement the regular **radiation-era AeST adiabatic initial-condition mode** before enabling memory (`eta > 0`). The current `chi_i=0, E_i=0` proxy must not be promoted to a physical AeST CMB prediction until this initial-condition gate is closed.

Memory remains OFF in all CLASS cosmology tests through v0.19n.
