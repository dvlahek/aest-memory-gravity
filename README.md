# AeST Memory Gravity

Research code and validation harness for an AeST-based history-dependent gravitational memory model.

Current integration gate: **v0.18 — CLASS source integration and zero-regression**.

The v0.18 workflow pins official CLASS v3.3.4, compiles pristine and patched trees, links the AeST-memory module, and verifies that the inactive extension produces no numerical regression before AeST cosmological perturbations are enabled.

See `THEORY_v018.md` and `.github/workflows/v018-class-compile.yml`.
